import React from 'react';

const Login = () => {
    return (
        <div>
                <h2>Login </h2>
                <button>Signin with Google</button> 
        </div>
    );
};

export default Login;